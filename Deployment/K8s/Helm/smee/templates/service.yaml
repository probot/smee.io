apiVersion: v1
kind: Service
metadata:
  name: {{ include "smee.fullname" . }}
  labels:
    {{- include "smee.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smee.selectorLabels" . | nindent 4 }}
