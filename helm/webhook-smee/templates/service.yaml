apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhook-smee.fullname" . }}
  labels:
    {{- include "webhook-smee.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webhook-smee.selectorLabels" . | nindent 4 }}
